spring:
  profiles:
    active: ${SPRING_PROFILES_ACTIVE:dev}
  application:
    name: discodeit
  jpa:
    open-in-view: false
    hibernate:
      ddl-auto: validate # 설정이 같은지만 체크
  servlet:
    multipart:
      maxFileSize: 10MB # 파일 하나의 최대 크기
      maxRequestSize: 30MB  # 한 번에 최대 업로드 가능 용량
  boot:
    admin:
      client:
        instance:
          name: discodeit

logging:
  level:
    root: info

# repo 구현체 선택
discodeit:
  storage: # storage 저장 방식
    type: ${STORAGE_TYPE:local}  # local/s3
    local:
      root-path: ${STORAGE_LOCAL_ROOT_PATH:.discodeit/storage}
    s3:
      access-key: ${AWS_S3_ACCESS_KEY}
      secret-key: ${AWS_S3_SECRET_KEY}
      region: ${AWS_S3_REGION}
      bucket: ${AWS_S3_BUCKET}
      presigned-url-expiration: ${AWS_S3_PRESIGNED_URL_EXPIRATION:10} # (기본값: 10분)

management:
  endpoints:
    web:
      exposure:
        include: [ "health", "info", "metrics", "loggers" ]
  endpoint:
    health:
      show-details: always
  info:
    env:
      enabled: true

info:
  application:
    name: ${spring.application.name}
    version: 1.7.0
  java:
    version: 17
  spring-boot:
    version: 3.5.10
  configs:
    datasource:
      url: ${spring.datasource.url}
      driver-class-name: ${spring.datasource.driver-class-name}
    jpa:
      ddl-auto: ${spring.jpa.hibernate.ddl-auto}
    storage:
      type: ${discodeit.storage.type}
      path: ${discodeit.storage.local.root-path}
    multipart:
      max-file-size: ${spring.servlet.multipart.maxFileSize}
      max-request-size: ${spring.servlet.multipart.maxRequestSize}

admin:
  enable: ${ADMIN_USER_ENABLE:true}
  email: ${ADMIN_USER_EMAIL:admin@admin.com}
  username: ${ADMIN_USER_NAME:admin}
  password: ${ADMIN_USER_PASSWORD:admin123!}
